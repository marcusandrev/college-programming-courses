# Create an array named array that contains integer values from 0 to 4
array = list(range(5))

# Prints the index and value of each element in array, using a for-each loop and the enumerate() function
for index, value in enumerate(array):
    print(f"a[{index}] = {value}")

